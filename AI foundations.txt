1. Mathematics & Foundations
Linear Algebra

Scalars, Vectors, Matrices, Tensors.

Matrix Operations: Transpose, Inverse, Trace, Determinant.

Dot Product, Cross Product, Hadamard Product.

Eigenvalues & Eigenvectors (The "Spectral Theory").

Singular Value Decomposition (SVD).

Calculus

Derivatives & Gradients.

Partial Derivatives & Jacobians.

The Chain Rule (Backpropagation foundation).

Hessian Matrices (Second-order optimization).

Probability & Statistics

Random Variables (Discrete vs. Continuous).

Probability Distributions: Gaussian (Normal), Bernoulli, Binomial, Poisson.

Bayes' Theorem (Posterior, Prior, Likelihood).

Expectation, Variance, Covariance.

Maximum Likelihood Estimation (MLE) vs. Maximum A Posteriori (MAP).

Information Theory

Entropy (Shannon Entropy).

Cross-Entropy Loss.

KL Divergence (Kullback-Leibler).

2. Classical Machine Learning
Supervised Learning

Linear Regression (OLS).

Logistic Regression (Sigmoid).

Support Vector Machines (SVM) & Kernels.

Decision Trees & Pruning.

K-Nearest Neighbors (KNN).

Naive Bayes Classifiers.

Ensemble Methods

Bagging (Random Forests).

Boosting (AdaBoost, XGBoost, LightGBM, CatBoost).

Unsupervised Learning

Clustering: K-Means, DBSCAN, Hierarchical Clustering.

Dimensionality Reduction: PCA (Principal Component Analysis), t-SNE, UMAP.

Optimization

Gradient Descent (Batch, Stochastic/SGD, Mini-batch).

Loss Functions: MSE, MAE, Huber Loss, Binary Cross Entropy.

3. Deep Learning Foundations
Core Architectures

Perceptron & Multi-Layer Perceptron (MLP).

Activation Functions: ReLU, Leaky ReLU, Sigmoid, Tanh, Softmax, GELU, Swish.

Training Dynamics

Backpropagation Algorithm.

Weight Initialization (Xavier/Glorot, He Initialization).

Optimizers: SGD with Momentum, RMSProp, Adam, AdamW.

Learning Rate Schedulers (Cosine Annealing, Step Decay).

Regularization

L1 & L2 Regularization (Weight Decay).

Dropout.

Batch Normalization vs. Layer Normalization.

Early Stopping.

4. Computer Vision (CV)
Foundations

Convolutions (Kernels, Stride, Padding).

Pooling (Max, Average, Global Average).

Architectures (The History)

LeNet-5, AlexNet, VGG-16.

ResNet (Residual Connections).

Inception (GoogLeNet).

MobileNet (Depthwise Separable Convolutions).

Advanced Tasks

Object Detection: YOLO (You Only Look Once), SSD, Faster R-CNN.

Segmentation: U-Net, Mask R-CNN.

Vision Transformers (ViT).

5. Natural Language Processing (NLP)
Text Representation

Tokenization (BPE, WordPiece, SentencePiece).

Embeddings: One-Hot, TF-IDF, Word2Vec (Skip-gram, CBOW), GloVe.

Sequence Models

RNNs (Recurrent Neural Networks).

LSTMs (Long Short-Term Memory) & GRUs.

Seq2Seq Models (Encoder-Decoder).

The Transformer

Attention Mechanism (Dot-product attention).

Self-Attention & Multi-Head Attention.

Positional Encodings (Sinusoidal vs. Learnable).

LayerNorm & Residual Connections in Transformers.

BERT (Encoder-only, Masked LM).

GPT (Decoder-only, Causal LM).

T5 (Encoder-Decoder).

6. Reinforcement Learning (RL)
Basics

Markov Decision Process (MDP): State, Action, Reward, Policy.

Exploration vs. Exploitation (Epsilon-Greedy).

Discount Factor (Gamma).

Algorithms

Value-Based: Q-Learning, Deep Q-Networks (DQN).

Policy-Based: REINFORCE, Policy Gradients.

Actor-Critic Methods: A2C, A3C.

Advanced: PPO (Proximal Policy Optimization), TRPO.

Reward Modeling (RLHF basics).

7. Generative AI (The Modern Era)
Generative Models

Autoencoders (AE) & Variational Autoencoders (VAE).

GANs (Generative Adversarial Networks): Generator vs. Discriminator.

Diffusion Models

Forward Process (Adding Noise).

Reverse Process (Denoising).

U-Net backbone in Diffusion.

Latent Diffusion (Stable Diffusion).

Large Language Models (LLMs)

Pre-training vs. Fine-tuning.

Instruction Tuning (Alpaca, Vicuna).

PEFT (Parameter-Efficient Fine-Tuning): LoRA, QLoRA.

RAG (Retrieval Augmented Generation).

Vector Databases (ChromaDB, Pinecone, FAISS).

8. Research Methodology (Crucial for Juniors)
Tools

PyTorch (The standard for research).

HuggingFace (Transformers, Datasets, Accelerate).

Weights & Biases (WandB) for experiment tracking.

LaTeX / Overleaf (For writing papers).

Skills

How to read ArXiv papers effectively.

Ablation Studies (Removing parts of the model to see what matters).

Reproducibility (Setting random seeds).

Benchmarking & Metrics (Precision, Recall, F1, AUC-ROC, BLEU, ROUGE, Perplexity).
